version: 2.1


executors:
  node:
    docker:
      - image: circleci/python:3.7-node-browsers-legacy

jobs:
  build:
    executor: node
    steps:
      - checkout:
          path: ~/project
      - run:
          command: echo 'test step'
          name: Test

workflows:
  build-test-deploy:
    jobs:
      - build